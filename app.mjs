import chalk from "chalk";

const log = console.log

log(chalk.blue("Why"), chalk.red("do"), chalk.magenta("programmers"), chalk.blueBright("prefer"), chalk.green("dark"), chalk.redBright("mode?"));

log(chalk.yellow("because"), chalk.black("lights"), chalk.cyan("attracts"), chalk.blue("bugs!"));